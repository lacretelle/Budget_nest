import { FunctionComponent } from "react";
import { FormAccount } from "../Components/FormAccount";

export const Account: FunctionComponent = () => {
	return (
		<main>
			<FormAccount type="updateAccount" />
		</main>
	);
};
