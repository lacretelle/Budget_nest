import { FunctionComponent } from "react";
import { FormAccount } from "../Components/FormAccount";

export const Signin: FunctionComponent = () => {
	return (
		<main>
			<FormAccount type="signin" />
		</main>
	);
};
